<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cleanapps.timekeeper"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="com.cleanapps.timekeeper.main.SplashScreen"
            android:label="@string/app_name" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.SplashAnimation"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.ActivityRegistration"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.SettingsTimekeeper"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.AboutTimekeeper"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.ActivityDetails"
            android:label="@string/app_name"
            android:theme="@style/Theme.Transparent" >
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.HelpTimekeeper"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.cleanapps.timekeeper.main.TimekeeperMain"
            android:label="@string/app_name" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>